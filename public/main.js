(function () {
  'use strict';
  /*function plural(n) {
    if (12 <= n && n <= 14)
      return (n + ' раз!');
    if (n % 10 === 2 || n % 10 === 3 || n % 10 === 4)
      return (n + ' раза!');
    return (n + ' раз!');
  }*/

  if (typeof window === 'object') {
    window.SIGN_IN();
  }
})();
