<fest:template xmlns:fest="http://fest.mail.ru" context_name="data">
    <button class="{data.attrs.class}"
            name="{data.attrs.name}"
            type="{data.attrs.type}"
    >
        <fest:value>data.text</fest:value>
    </button>
</fest:template>