<fest:template xmlns:fest="http://fest.mail.ru" context_name="attributes">
    <button class="{attributes.class}__input{attributes.name}"
           type="{attributes.type}"
           required='{attributes.required}'
           name="{attributes.name}"
           placeholder="{attributes.placeholder}">
        <fest:value>attributes.text</fest:value>
    </button>
</fest:template>